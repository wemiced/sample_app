<% content_for(:title, 'All users') %>
<h1>All users</h1>

<ul class="users">
  <%= will_paginate @users %>
    <ul class="users">
      <% @users.each do |user| %>
          <%= render user %>
      <% end %>
    </ul>
  <%= will_paginate %>

</ul>
